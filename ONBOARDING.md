supabase client lives in veiss-mobile-app: lib/supabaseClient.js
we currently hardcode the supabase url and anon key there, but later you can move it to env file.

realtime.js is in veiss-mobile-app: services/
